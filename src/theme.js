import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: '#f7f5f4'
        },
        secondary: {
            main: '#40ff00'
        }
        
    }
})

export default theme;